<%= form_for(@check_app) do |f| %>
  <% if @check_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@check_app.errors.count, "error") %> prohibited this check_app from being saved:</h2>

      <ul>
      <% @check_app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
